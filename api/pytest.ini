[pytest]
testpaths = tests
addopts = -q --tb=short
asyncio_mode = auto
# pooled asyncpg connections are bound to the loop that created them; run the whole suite on one loop
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
